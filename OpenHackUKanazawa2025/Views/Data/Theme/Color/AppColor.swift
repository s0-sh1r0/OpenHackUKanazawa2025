import SwiftUI

enum AppColor {
    enum Background {
        static let purple = Color(hex: "#563798")
        static let white = Color(hex: "#FFFFFF")
        static let black = Color(hex: "#000000")
    }
    
    enum Text {
        static let purple = Color(hex: "#563798")
        static let white = Color(hex: "#FFFFFF")
        static let black = Color(hex: "#000000")
    }
    
    enum SystemImage {
        static let orange = Color(hex: "#FF6A00")
        static let purple = Color(hex: "#563798")
        static let white = Color(hex: "#FFFFFF")
        static let black = Color(hex: "#000000")
    }
}
